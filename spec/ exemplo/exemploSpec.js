describe("suite de testes de exemplo com NodeJs", () => {
	it("Deve demosntrar o uso do jasmine com o NodeJs", () => {
		expect(true).toEqual(true);
	})

	it("Deve demosntrar o uso do karma em modo de dev", () => {
		expect(1).toEqual(1);
	})
})